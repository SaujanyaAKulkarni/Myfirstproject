# ask a user for name 
# print count of each words
# output :
      # S : 1
      # H : 1
      # A : 1
      # N : 1
temp_var = ""
i = 0
while i < len(name):
    if name[i] not in temp_var :
    temp_var += name[i]
    print(f"{name[i]} :{name.count(name[i])}")
 i +=1
      
      
      
